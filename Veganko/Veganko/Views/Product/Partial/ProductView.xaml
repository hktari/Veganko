<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="Veganko.Views.Product.Partial.ProductView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Veganko.Controls"
    xmlns:converters="clr-namespace:Veganko.Converters"
    xmlns:local="clr-namespace:Veganko"
    xmlns:models="clr-namespace:Veganko.Models">
    <ContentView.Resources>
        <converters:StringCaseFormatter x:Key="StringCaseConverter" ToUpperCase="True" />
    </ContentView.Resources>
    <ContentView.Content>
        <Grid
            ColumnSpacing="10"
            HeightRequest="113"
            RowSpacing="0"
            VerticalOptions="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Frame
                Grid.RowSpan="2"
                Padding="1"
                BorderColor="Black"
                CornerRadius="0"
                HorizontalOptions="Center"
                IsClippedToBounds="True"
                VerticalOptions="Center">
                <Image
                    Aspect="AspectFill"
                    HeightRequest="113"
                    HorizontalOptions="Center"
                    Source="{Binding Image}"
                    VerticalOptions="Center"
                    WidthRequest="113" />
            </Frame>
            <Label Grid.Column="1" MaxLines="3">
                <Label.FormattedText>
                    <FormattedString>
                        <Span FontAttributes="Bold" Text="{Binding Name, Converter={StaticResource StringCaseConverter}, Mode=OneWay}" />
                        <Span Text="&#10;" />
                        <Span Text="{Binding Description, Mode=OneWay}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <controls:EnumImageView
                Grid.Row="1"
                Grid.Column="1"
                HorizontalAlignment="Start"
                x:TypeArguments="models:ProductClassifier"
                ImageSource="{x:Static local:EnumImages.ProductClassifierImages}"
                Source="{Binding ProductClassifiers}"
                VerticalOptions="End"
                ViewSize="50" />
        </Grid>
    </ContentView.Content>
</ContentView>