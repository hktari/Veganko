<?xml version="1.0" encoding="utf-8" ?>
<views:BaseContentPage
    x:Class="Veganko.Views.ProfilePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:partial="clr-namespace:Veganko.Views.Profile.Partial"
    xmlns:productPartial="clr-namespace:Veganko.Views.Product.Partial"
    xmlns:views="clr-namespace:Veganko.Views"
    xmlns:vm="clr-namespace:Veganko.ViewModels"
    x:Name="root">
    <ContentPage.BindingContext>
        <vm:ProfileViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <ResourceDictionary>
            <Thickness x:Key="ViewPadding">
                <x:Arguments>
                    <x:Double>10</x:Double>
                </x:Arguments>
            </Thickness>
            <DataTemplate x:Key="RemovableProductTemplate">
                <ViewCell>
                    <ViewCell.ContextActions>
                        <MenuItem
                            Command="{Binding Source={x:Reference root}, Path=DeleteProdModReqCommand}"
                            CommandParameter="{Binding .}"
                            IsDestructive="True"
                            Text="Delete" />
                    </ViewCell.ContextActions>
                    <productPartial:ProductView Margin="10,0" />
                </ViewCell>
            </DataTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <ListView
            BackgroundColor="{StaticResource DarkBackgroundColor}"
            CachingStrategy="RetainElement"
            HasUnevenRows="False"
            ItemTemplate="{StaticResource RemovableProductTemplate}"
            ItemsSource="{Binding ProductModRequests}"
            RefreshCommand="{Binding}"
            RowHeight="143"
            SeparatorVisibility="None">
            <ListView.Header>
                <Grid ColumnSpacing="0" RowSpacing="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <partial:UserProfileView />
                    <Button
                        Margin="{StaticResource ViewPadding}"
                        Clicked="OnLogoutClicked"
                        HorizontalOptions="End"
                        Style="{StaticResource NeutralBtn}"
                        Text="Logout"
                        VerticalOptions="End" />
                    <Button
                        Margin="{StaticResource ViewPadding}"
                        Clicked="OnSaveClicked"
                        HorizontalOptions="Start"
                        IsVisible="{Binding IsDirty}"
                        Style="{StaticResource PositiveBtn}"
                        Text="Shrani spremembe"
                        VerticalOptions="End" />
                    <ImageButton
                        Margin="{StaticResource ViewPadding}"
                        BackgroundColor="White"
                        Command="{Binding HelpCommand}"
                        HeightRequest="38"
                        HorizontalOptions="End"
                        VerticalOptions="Start"
                        WidthRequest="38"
                        Source="ico_help.png" />
                    <BoxView Grid.Row="1" BackgroundColor="{StaticResource PrimaryDark}" />
                    <Label
                        Grid.Row="1"
                        Margin="10,20"
                        FontFamily="{StaticResource QuicksandFont}"
                        FontSize="17"
                        Text="Tvoje prošnje za spremembe produktov so tukaj"
                        TextColor="White" />
                </Grid>
            </ListView.Header>
        </ListView>
    </ContentPage.Content>
</views:BaseContentPage>