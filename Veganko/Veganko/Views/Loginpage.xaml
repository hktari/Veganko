<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Veganko.Views.Loginpage">
    <ContentPage.Content>
        <StackLayout>
			<!--<StackLayout VerticalOptions="CenterAndExpand">
				<Label Text="Username"/>
				<Entry Text="{Binding User.Username}"/>
				<BoxView HeightRequest="1" HorizontalOptions="FillAndExpand" BackgroundColor="DarkGray" Margin="0, 5"/>
				<Label Text="Password"/>
				<Entry Text="{Binding User.Password}" IsPassword="True"/>
			</StackLayout>-->
			<Button Command="{Binding LoginCommand}" Text="Login" HorizontalOptions="Fill" VerticalOptions="Center"/>
		</StackLayout>
    </ContentPage.Content>
</ContentPage>